[package]
name = "forge-kernel-agent"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Versioned contracts and fail-closed evaluation loop for execution-driven kernel search"

[dependencies]
forge-core = { path = "../forge-core" }
serde = { version = "1", features = ["derive"] }

[lints]
workspace = true
