[package]
name = "forge-kernel-search"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
forge-core = { path = "../forge-core" }
forge-kernel-agent = { path = "../forge-kernel-agent" }
serde = { version = "1", features = ["derive"] }

[lints]
workspace = true
